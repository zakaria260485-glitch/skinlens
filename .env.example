# SERVER-ONLY. Leave empty in tracked files. Configure a sensitive Vercel
# environment variable only after independent validation and code review.
# A key by itself does not enable analysis in the public Validation Edition.
DERMIQ_API_KEY=
